-- Conectar a MySQL
mysql -u facuu13 -p

-- Seleccionar la base de datos
USE sensor_data;

-- Verificar los registros a eliminar
SELECT * FROM mediciones WHERE temperature IS NULL OR humidity IS NULL;

-- Eliminar los registros seleccionados
DELETE FROM mediciones WHERE temperature IS NULL OR humidity IS NULL;

-- Verificar que los registros se han eliminado
SELECT * FROM mediciones;


-- Vaciar la tabla
TRUNCATE TABLE mediciones;


sudo mysql -u root -p

CREATE DATABASE sensor_data;
CREATE USER 'tu_usuario_mysql'@'localhost' IDENTIFIED BY 'tu_contraseña_mysql';
GRANT ALL PRIVILEGES ON sensor_data.* TO 'tu_usuario_mysql'@'localhost';
FLUSH PRIVILEGES;

USE sensor_data;

CREATE TABLE mediciones (
    id INT AUTO_INCREMENT PRIMARY KEY,
    sensor_id VARCHAR(50),
    temperature FLOAT,
    humidity FLOAT,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    topic VARCHAR(255),
    led BOOLEAN
);
